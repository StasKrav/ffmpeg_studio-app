services:
  app:
    build: .
    container_name: ffmpeg-studio
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    restart: unless-stopped
    environment:
      - PORT=3000